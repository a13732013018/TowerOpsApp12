# 2026-04-05 工作日志

## 门禁数据远程开门时间功能（已完成）
- 功能：运维日常Tab → 门禁数据，增加远程开门时间列
- 匹配优先级：先蓝牙（30分钟内合格即通过），蓝牙全不合格/无记录才查远程开门时间
- 修改文件：
  - DoorDataFragment.java: DoorAlarmRow增加remoteOpenTime/remoteDiffMinutes/qualifyReason字段；fetchAndMatch新增Step 5.5（远程匹配）；SortCol枚举加REMOTE_TIME；CSV导出加远程时间列；新增queryFsuidByStationName()辅助方法
  - DoorDataAdapter.java: ViewHolder增加tvRemoteTime；显示远程时间（合格橙色）；合格标签区分"合格(远程)"
  - fragment_door_data.xml: 表头增加"远程时间"列（weight=1.3）
  - item_door_data.xml: 列表项增加tvDoorRemoteTime（weight=1.3）
- 核心依赖：AccessControlApi.queryFsuList(stationName) + getRemoteOpenTime(fsuid) 方法均已存在
- 所有文件lint检查通过（0 diagnostics）

## 远程开门时间参数改造（2026-04-05 晚）
- 用户确认：FSU ID（col[16]）和门禁ID均为14位数字
  - FSU ID：第7-9位固定为"438"（OMMS 历史门禁告警 col[16] 设备ID）
  - 门禁ID：第7-9位固定为"499"（listEntrance.xhtml 门禁管理页返回 checkbox id）
- 改造内容：
  1. DoorAlarmRow新增 fsuid 字段（存col[16]值）
  2. parseAlarmRows已提取col[16]到RawAlarm.deviceId，fetchAndMatch时取同站首条非空deviceId写到newestRow.fsuid
  3. Step 5.5直接用row.fsuid（438特征）而不再调queryFsuidByStationName；col[16]为空时兜底仍用站名查
  4. AccessControlApi新增 queryEntranceDoorId(fsuid)：POST listEntrance.xhtml，传j_id11=FSU ID，返回HTML中找第7-9位=499的14位数字 = 门禁ID
  5. AccessControlApi新增 getRemoteOpenTimeByFsuid(fsuid)：fsuid→queryEntranceDoorId→门禁ID→POST listEntrance查时间
  6. getRemoteOpenTime()开头加快速路由：14位且438特征直接调getRemoteOpenTimeByFsuid
- 所有文件lint检查通过（0 diagnostics）

## 远程开门时间匹配逻辑优化（2026-04-05 晚）
- 用户需求：远程开门时间也要和告警时间取差值，取与告警时间**最近**的那条远程开门时间
- 合格规则：蓝牙和远程**只要有一个**满足≤30分钟就合格，两个都不满足才不合格
- 改造内容：
  1. AccessControlApi新增 extractAllTimesFromHtml()：从HTML响应中提取所有时间字符串（返回List）
  2. AccessControlApi新增 getAllRemoteOpenTimesByFsuid(fsuid)：返回所有远程开门时间列表（非单条）
  3. Step 5.5改为：获取所有远程时间 → 逐条与告警时间算差值 → 取差值最小（最近）的那条 → 判断是否≤30分钟

## 匹配逻辑重构：从按站聚合改为逐条告警（2026-04-05 晚）
- 用户要求：每条历史门禁告警都要独立匹配，不是每站只展示一条
- 改造前：按站名分组，每站只留最新告警一条，蓝牙合格则跳过远程
- 改造后：逐条告警匹配，每条先蓝牙（取与告警时间最近的蓝牙时间）→ 不合格再远程（取与告警时间最近的远程时间）
- 合格规则：蓝牙和远程只要有一个≤30分钟就合格
- 性能优化：同一 FSU ID 的远程开门记录只查一次，结果缓存复用（remoteTimesCache）
- 删除了多余的 Step 4（btByName 已在 Step 3 从 lanyaList 构建好）
- 展示变化：列表从"每站一条"变为"每条告警一行"，状态栏显示"XX条告警"而非"XX站"

- 设备离线：独立顶层Tab → 数运工单子Tab（position 4）
- 省内工单处理及时率：独立顶层Tab → 指标查询子Tab（position 8）
- 顶层Tab数量：11 → 9
- 排序按钮显示修复（WorkOrderFragment/DeviceOfflineFragment）
- 区县Spinner扩大宽度（90dp / dropDownWidth 180dp）
